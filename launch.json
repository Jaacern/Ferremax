{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run Ferremas Backend",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/backend/run.py",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
